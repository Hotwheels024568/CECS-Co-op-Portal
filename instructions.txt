To create
	py -3.12 -m venv venv

In venv\Scripts\Activate.ps1, put the following at the end of the file (before the signature block):
	# Enforces python to see the src folder and run code as a module
	$env:PYTHONPATH = "$env:VIRTUAL_ENV\..\src;$env:PYTHONPATH"

put the following at the end of the bat file (before :END):
	rem Enforces python to see the src folder and run code as a module
	set PYTHONPATH=%VIRTUAL_ENV%\..\src;%PYTHONPATH%

To run: Open a terminal in the project's root folder
	To start the venv in cmd
		venv\Scripts\activate.bat
	
	To start the venv in PowerShell
		venv\Scripts\Activate.ps1
	
	To run
		python -m src.(folder).(file)
		python -m src.database.manage
		uvicorn src.backend.main:app --reload --port 8000 --no-use-colors

To end
	deactivate

To install & share
	pip install (package)
	pip install -r requirements.txt
	pip freeze > requirements.txt

React project
	Run the following a the directory of choice
		npm create vite@latest
			
			project name:	frontend
			framework:		React
			variant:		React Router v7 (Choose from JavaScript, TypeScript, or Router (TS, default code for page routing))
	
	npm install
	npm run dev
	npm run build
